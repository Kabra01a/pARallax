# Copy to `app/.env` and edit. Never commit the real .env.

# LAN address of the computer running the local server.
# Must be reachable from your phone - localhost will NOT work on a device.
EXPO_PUBLIC_SERVER_URL=http://192.168.1.29:8080

EXPO_PUBLIC_REQUEST_TIMEOUT_MS=30000
