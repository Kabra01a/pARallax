# Requires Python 3.10-3.12.
#
# Upper bound is real: onnxruntime publishes no wheels for CPython 3.13 or 3.14
# on any platform tag, and it is the inference engine rembg runs on. On a newer
# interpreter, `pip install` fails with no candidate found.
#
#   /opt/homebrew/bin/python3.12 -m venv venv
#
# Ranges rather than exact pins: onnxruntime and Pillow ship per-Python-version
# wheels, and hard pins break on interpreters with no matching prebuilt wheel.

rembg>=2.0.65,<3
onnxruntime>=1.20
Flask>=3.0,<4
python-dotenv>=1.0
Pillow>=10.2
